# Auckland cameras

Canonical catalog used by the web app lives in `src/config/cameras.js` (non-www `https://windsurf.co.nz/` stills and charts).

- `cameras.json` — audit dump (links, notes, mixed HTTP/HTTPS)
- `auckland-wind.html` — Live | Forecast prototype that was imported; equal-height still/chart is implemented in `public/live.css`, not this file
